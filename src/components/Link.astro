---
type Props = {
    href?: string;
    rel?: string;
    target?: string;
}

const { href, rel, target } = Astro.props;

---

<a
    class="link link-secondary transition-colors"
    target={target}
    rel={rel}
    href={href}
>
  <slot />
</a>
